package commands;

import storage.Authentication;
import commands.interfaces.Command;

public class IsUser implements Command<Authentication> {

    public void execute(Authentication authentication, boolean muteMode) {

    }

}
